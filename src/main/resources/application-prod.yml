spring:
  jpa:
    database-platform: ${HIBERNATE_DIALECT:org.hibernate.dialect.MariaDBDialect}
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: ${SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: false

cafetron:
  dev-seed:
    enabled: false
